[package]
name = "spike-uds"
version = "0.0.0"
edition = "2021"
publish = false

# Phase 0 spike #2 (PLAN.md): does tiny_http 0.12 serve over a Unix socket,
# and does unblock() actually unblock recv()? Both are load-bearing for
# `harbor serve` (UDS face) and its drain-on-SIGTERM shutdown path.
[dependencies]
tiny_http = "0.12"

[[bin]]
name = "spike-uds"
path = "main.rs"
